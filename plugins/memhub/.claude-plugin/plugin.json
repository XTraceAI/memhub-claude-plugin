{
  "name": "memhub",
  "description": "Auto-capture Claude Code sessions into MemHub team memory. Incremental flushes on commit/PR events (PostToolUse hooks) plus a SessionEnd backstop read the transcript and save it via the memhub-staging MCP, which runs tool-aware (agentic) extraction of facts, episodes, and artifacts with watermark-based delta dedup. A PreToolUse hook surfaces situated team directives (lessons/procedures) before each Edit/Write/Bash by firing the memhub recall_directives symbol-tripwire on the in-flight call and injecting any hits as context (a client-side precision gate drops directives whose triggers don't concretely match the call, e.g. an over-broad repo-name trigger). Ships skills for terminal artifact uploads (save-artifact), on-demand session import (import-session), team-memory recall (search-memory), teammate session handoff (handoff-session), spec-driven development on versioned spec artifacts (spec), and PR babysitting (pr-babysit) — a hook arms a self-paced loop after `gh pr create` that fixes review-bot findings and saves the fixing process to the repo's agent brain.",
  "version": "0.9.1",
  "author": {
    "name": "XTrace",
    "email": "felixmeng@xtrace.ai"
  },
  "homepage": "https://memhub.xtrace.ai",
  "keywords": [
    "memory",
    "memhub",
    "xtrace",
    "session-capture"
  ]
}
